/**
 * @file AI single term content generation prompt helpers.
 *
 * No hardcoded default content — prompts are loaded from site settings
 * (default-settings.json). Placeholder reference:
 * - {{term}} — the term name
 * - {{definitionHint}} — short hint/definition from discovery
 * - {{context}} — article slug for context
 * - {{language}} — target language code ("zh" or "en")
 */

export {};
