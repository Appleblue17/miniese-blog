/**
 * @file AI wiki term discovery prompt helpers.
 *
 * No hardcoded default content — prompts are loaded from site settings
 * (default-settings.json). Placeholder reference:
 * - {{content}} — the article content to scan
 * - {{language}} — the article's language ("Chinese" or "English")
 */

export {};
